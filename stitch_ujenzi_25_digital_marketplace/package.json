{
  "name": "stitch-ujenzi-25-digital-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for UJENZI 25 - tooling to build and deploy to GitHub Pages",
  "scripts": {
    "dev": "live-server --port=3000 --open=./",
    "build": "node build.js",
    "start": "npx http-server dist -p 3000",
    "deploy": "echo Use GitHub Actions (see .github/workflows/gh-pages.yml)"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "live-server": "^1.2.2"
  }
}
